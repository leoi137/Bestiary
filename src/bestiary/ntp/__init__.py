"""Next-token prediction: imitate a recorded teacher with a causal transformer.

Stage 1 of the imitation track: blind (proprioception-only), from scratch,
trained on the tapes `bestiary.isaac.record_spot` produces under the contract
in `research/SPOT_ROLLOUTS_SPEC.md`.
"""
